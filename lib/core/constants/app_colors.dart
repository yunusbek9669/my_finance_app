import 'package:flutter/material.dart';

/// Ilova bo'ylab ishlatiladigan ranglar
/// Bir joyda saqlash - o'zgartirish oson bo'ladi
class AppColors {
  // Asosiy ranglar
  static const Color primary = Color(0xFF6C63FF); // Binafsha
  static const Color secondary = Color(0xFF00D4FF); // Moviy

  // Daromad va xarajat ranglari
  static const Color income = Color(0xFF00C853); // Yashil
  static const Color expense = Color(0xFFFF5252); // Qizil

  // Background ranglar
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Text ranglar
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status ranglar
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Border ranglar
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);

  // Qorong'u rejim uchun (kelajakda)
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
}